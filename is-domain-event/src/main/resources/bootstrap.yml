#默认使用7224端口
server:
  port: 7224

spring:
  application:
    name: is-domain-event

management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
