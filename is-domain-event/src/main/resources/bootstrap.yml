#默认使用7224端口
server:
  port: 7224

spring:
  application:
    name: is-domain-event
  profiles:
    active: dev # 表示开发环境
  #    active: test # 表示测试环境
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml

management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
